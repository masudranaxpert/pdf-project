"use client";

import { DashboardContent } from '@/components/dashboard-content';

/**
 * Dashboard Page
 * Main view for PDF tools, now wrapped in the persistent layout
 */
export default function DashboardPage() {
    return <DashboardContent />;
}
